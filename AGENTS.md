# Project Guidelines

- **Python Environment**: Always use the `venv/` directory for Python virtual environments (e.g., `source venv/bin/activate`), as defined in the README. Do NOT create or use `.venv/`.
